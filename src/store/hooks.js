import { useDispatch, useSelector } from "react-redux";

/** @type {typeof useDispatch} */
export const useAppDispatch = useDispatch;

/** @type {typeof useSelector} */
export const useAppSelector = useSelector;
